## Plan

1. **Create a patched `widget.js` artifact from the live hosted script**
   - Use the currently hosted `https://widget.fuqah.net/widget.js` as the base, since this is the file still rendering the leftover welcome bubble.
   - Bump the header/version label to `4.7.2` so it is clear which script is deployed.

2. **Fix the master off-state inside `widget.js` itself**
   - Add a single cleanup helper that removes every widget element from the storefront DOM:
     - `#fq-widget-root`
     - `#fq-bubble`
     - `.fq-welcome-bubble`
     - `#fq-chat-window`
     - `#fq-overlay`
     - any modal/overlay classes generated by the widget
   - In `fetchConfig`, fetch `widget-config` with `cache: 'no-store'` and a timestamp query param.
   - If `bubble_visible === false`, set `settings.bubbleVisible = false`, run cleanup, and prevent `buildWidget()` from running at all.

3. **Prevent welcome bubble resurrection when closing chat**
   - Update `renderWelcomeBubble()` to immediately return when `settings.bubbleVisible === false`.
   - Update `closeChat()` and `fullClose()` so they only restore the launcher/welcome bubble when the master visibility setting is still enabled.
   - Guard `openChat()` so stale click handlers cannot open a hidden widget.

4. **Keep click counting accurate**
   - Preserve the existing behavior where only actual launcher/open clicks send `bubble.click` to `widget-events`.
   - Ensure close/reopen and welcome bubble clicks still count exactly once per user open action, not on passive render.

5. **Deliver/deploy the patched file**
   - Generate the patched `widget.js` in `/mnt/documents/widget-4.7.2.js` for inspection/download.
   - If you want me to update the actual CDN/Hostinger-hosted `https://widget.fuqah.net/widget.js`, I’ll need the upload/deployment route available in this project or credentials/workflow for that host; otherwise I can only prepare the corrected file and update the in-repo widget sources.

6. **Verify**
   - Re-fetch the patched script and confirm it contains the new off-state guards.
   - Test that `bubble_visible=false` means no welcome bubble, launcher, chat page, overlay, or root widget remains.
   - Test that `bubble_visible=true` shows the widget again after a hard refresh and clicks still post `bubble.click`.